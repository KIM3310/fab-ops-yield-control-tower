apiVersion: v1
kind: Service
metadata:
  name: semiconductor-ops-platform
  labels:
    app: semiconductor-ops-platform
spec:
  type: ClusterIP
  selector:
    app: semiconductor-ops-platform
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
