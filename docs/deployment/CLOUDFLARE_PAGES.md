# Cloudflare Pages Deploy

- Root directory: `.`
- Build command: `(none)`
- Output directory: `site`

Notes:

- This deploy is a static public summary surface.
- The full fab operations backend/runtime remains a separate service concern.
